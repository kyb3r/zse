[server]
addr =  # no need to change
port =  # no need to change
user =  # your zID

[auth] # password auth
type = password # dont change
password =  # optional(but recommand), if not provided this field, the program will prompt for password

# [auth] # key auth
# type = "key"
# private_key_path = "~/.ssh/id_ed25519" # required for key auth
# # public_key_path = "/path/to/public/key" # optional
# # passphrase = "secret" # optional

# [auth] # agent auth
# type = "agent"